<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/a</item>
        <item name="colorPrimaryDark">@color/a</item>
        <item name="colorAccent">@color/a</item>
    </style>
    <style name="QuestionActivityTheme" parent="Theme.AppCompat">
    </style>
    <style name="Main2ActivityTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoDisplay">false</item>
        <item name="colorPrimary">@color/a</item>
        <item name="colorPrimaryDark">@color/a</item>
        <item name="colorAccent">@color/a</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Divider">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="background">@color/colorPrimary</item>
    </style>
    <style name="Divider_invisible">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>
</resources>
